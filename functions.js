export default functions = {
    To2(a) {
        retorno = []
        for(x = 1; x <= a; x++) {
            retorno.push(2)
        }
    },
    To1(a) {
        retorno = []
        for(x = 1; x <= a; x++) {
            retorno.push(1)
        }
        return retorno
    }
}